# Variables no sensibles para el entorno STAGING.
# Los secretos se guardan en Google Secret Manager y se inyectan con --set-secrets.
# Copia este archivo a env/staging.yaml si necesitas valores locales de referencia.

FIRESTORE_PREFIX: "staging"
LOG_EXECUTION_ID: "true"
THRIO_KB_ID: "XXXXXXXXXXXXXXXXXXXXXXXX"
